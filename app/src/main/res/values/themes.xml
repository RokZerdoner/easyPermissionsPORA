<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.EasyPermissionsPORA" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="My.Theme.EasyPermissionsPORA" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Define your custom styling attributes here -->
        <item name="android:windowBackground">@drawable/round_dialog_background</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowSoftInputMode">stateVisible|adjustResize</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_gravity">center</item>
        <!-- Add more styling attributes as needed -->
    </style>

    <style name="Theme.EasyPermissionsPORA" parent="Base.Theme.EasyPermissionsPORA" />
</resources>